# Telegram-бот: переводчик грузинского транслита

Бот переводит между тремя вариантами письма/языка, даёт краткий разбор фраз,
хранит личный словарь сохранённых переводов и присылает шпаргалку по
транслиту. Режим **GEO Translit ➡️ RU** использует двухэтапную обработку со
смарт-маршрутизацией между бесплатным Google Translate и Gemini API — см.
раздел «Как это работает» ниже.

## Режимы перевода

1. **GEO Translit ➡️ RU** (режим по умолчанию) — грузинский чат-транслит
   (карто-латиница, например `Gacnobebt, rom ganaxlda...`) → русский. Работает
   через двухэтапный пайплайн (см. ниже), а не прямым вызовом Gemini.
2. **RU ➡️ GEO Translit** — русский текст → грузинский чат-транслит (через Gemini).
3. **GEO Translit ➡️ ქართული** — грузинский транслит → грузинский алфавит
   (мхедрули), через Gemini.

Режим переключается командой `/settings` через inline-клавиатуру и хранится
отдельно для каждого пользователя.

## Тональность перевода

Там же, в `/settings`, можно выбрать тональность для режимов (1) и (2):

- 👔 **Официальный** — формулы вежливости, Вы-форма, терминология банков,
  госучреждений и юридических документов.
- 💬 **Разговорный** (по умолчанию) — естественная разговорная речь, как в
  мессенджерах.

Тональность подмешивается в системный промт Gemini дополнительной инструкцией
(`OFFICIAL_STYLE` / `CASUAL_STYLE` в `prompts.py`). Действует только тогда,
когда перевод действительно идёт через Gemini — для режима (3) и для простых
фраз режима (1), переведённых бесплатным Google Translate, тональность не
применяется (Google Translate не принимает стилистические инструкции).

## Кнопки под переводом

- **🇬🇪 Исходный текст (Мхедрули)** (только режим 1) — мгновенно присылает
  исходную фразу, сконвертированную в грузинский алфавит локально (Этап 1
  пайплайна, без каких-либо API-вызовов).
- **🔍 Разбор** (только режим 1) — короткий разбор: тон фразы, нюансы и
  контекст, ключевые слова с переводом. Присылается отдельным сообщением
  (вызывает Gemini).
- **⭐ Сохранить** (все режимы) — сохраняет пару «исходный текст → перевод» в
  личный словарь пользователя.

## Личный словарь (`/dictionary`, кнопка «📚 Мой словарь»)

Сохранённые фразы хранятся в SQLite (таблица `vocabulary`: `id`, `user_id`,
`original_text`, `translated_text`, `created_at`) и доступны по команде
`/dictionary` или кнопке **«📚 Мой словарь»** в меню под полем ввода — список
с пагинацией (по 5 записей на страницу) и кнопкой удаления у каждой записи.

## Шпаргалка транслита (`/alphabet`, кнопка «📖 Шпаргалка транслита»)

Статичное сообщение-справочник по самым частым буквосочетаниям (kh, dz, ts,
ch, sh, gh, zh) — отправляется мгновенно, **без** обращения к каким-либо API.

> **Про модель Gemini:** изначально в проекте использовалась `gemini-2.5-flash`,
> но Google закрыла доступ к ней для новых пользователей API — при вызове
> возвращается `404 NOT_FOUND` с рекомендацией переходить на `gemini-3.6-flash`.
> Именно эта модель используется во всех вызовах Gemini в этом боте (константа
> `GEMINI_MODEL` в `main.py`), в том числе на "сложной" ветке смарт-роутинга —
> сознательно не `gemini-2.5-flash`, чтобы не наступить на те же грабли ещё раз.
> Если появится похожая ошибка `404 NOT_FOUND` с упоминанием другой модели —
> обновите `GEMINI_MODEL` на модель из текста ошибки (актуальный список:
> https://ai.google.dev/gemini-api/docs/models).

## Структура проекта

```
.
├── main.py             # хендлеры aiogram, оркестрация пайплайна, точка входа
├── prompts.py          # системные промты для Gemini + текст шпаргалки
├── modes.py            # конфигурация режимов/тональности, сборка промта
├── transliteration.py  # локальный конвертер латиница -> мхедрули (Этап 1)
├── smart_routing.py    # is_simple_text (Этап 2) + перевод через Google Translate
├── db.py               # SQLAlchemy (async) + SQLite, таблица vocabulary
├── keyboards.py        # общие клавиатуры (меню, /settings, кнопки под переводом)
├── vocabulary.py       # отрисовка страниц личного словаря (пагинация)
├── requirements.txt    # зависимости
├── .env.example        # шаблон конфига
├── Dockerfile          # для деплоя в облако (Railway/Render/Docker)
├── .dockerignore
├── .gitignore
└── README.md
```

## Установка

1. Склонируйте/скопируйте проект и перейдите в его директорию.

2. Создайте и активируйте виртуальное окружение (рекомендуется):

   ```bash
   python -m venv venv
   source venv/bin/activate      # Linux / macOS
   venv\Scripts\activate         # Windows
   ```

3. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

4. Скопируйте `.env.example` в `.env` и заполните значения:

   ```bash
   cp .env.example .env
   ```

   ```env
   TELEGRAM_BOT_TOKEN=токен_от_BotFather
   GEMINI_API_KEY=ключ_от_Gemini_API
   DATABASE_PATH=bot.db
   ```

   - `TELEGRAM_BOT_TOKEN` — получить у [@BotFather](https://t.me/BotFather) (`/newbot`).
   - `GEMINI_API_KEY` — получить в [Google AI Studio](https://aistudio.google.com/apikey).
   - `DATABASE_PATH` — необязательно, путь к файлу SQLite для словаря (см.
     ниже про персистентность на Railway).

   Ключ для Google Translate не нужен — `deep_translator` использует
   бесплатный публичный интерфейс `translate.google.com`, без регистрации и
   без API-ключа.

## Запуск

```bash
python main.py
```

При первом запуске автоматически создаётся файл SQLite (таблица `vocabulary`).
Бот запустится в режиме long polling. Отправьте ему `/start` — появится меню
с кнопками словаря и шпаргалки, а также подскажет текущий режим/тональность.
Команда `/settings` открывает переключатель режима и тональности.

## Деплой в облако, чтобы бот работал 24/7 без вашего компьютера

Бот использует long polling (сам стучится к Telegram), поэтому ему не нужен
открытый HTTP-порт и статический IP — подходит почти любой облачный хостинг,
который умеет запускать Docker-контейнер или произвольный процесс. Ниже —
самый простой вариант: **Railway**.

> Почему Railway: он умеет запускать «background worker» (процесс без
> публичного URL) прямо из `Dockerfile`, не требует карты для старта и на
> практике такой лёгкий бот укладывается в пару долларов в месяц. Render.com
> тоже подойдёт, но там background worker (без HTTP-порта) доступен только на
> платных планах от $7/мес — на Render бесплатен только полноценный «Web
> Service», которому нужен открытый порт.

### Вариант A — через GitHub (проще всего)

1. Создайте репозиторий на GitHub и залейте туда все файлы проекта **кроме**
   `.env` и `*.db` (они и так игнорируются через `.gitignore` — реальные
   токены и локальная база не должны попадать в репозиторий).
2. Зайдите на [railway.com](https://railway.com), зарегистрируйтесь (можно
   через GitHub, без карты).
3. **New Project → Deploy from GitHub repo** → выберите ваш репозиторий.
   Railway сам увидит `Dockerfile` и соберёт образ.
4. Откройте вкладку **Variables** созданного сервиса и добавьте:
   - `TELEGRAM_BOT_TOKEN` = ваш токен от BotFather
   - `GEMINI_API_KEY` = ваш ключ Gemini API
5. Дождитесь окончания деплоя (вкладка **Deployments**) и откройте
   **Logs** — там должна появиться строка `Бот запущен, начинаю polling...`.
6. Готово — бот работает в облаке и отвечает независимо от того, включён ли
   ваш компьютер.

Если у сервиса включена генерация публичного домена (Settings → Networking) —
её можно отключить, она не нужна для этого бота.

### Вариант B — через Railway CLI (без GitHub)

```bash
npm install -g @railway/cli   # или через brew/curl — см. docs.railway.com
railway login
cd telegram-georgian-translator
railway init
railway variables --set "TELEGRAM_BOT_TOKEN=ваш_токен" --set "GEMINI_API_KEY=ваш_ключ"
railway up
```

Railway соберёт образ по `Dockerfile` из текущей папки и задеплоит его.

### Персистентность словаря на Railway (важно!)

Диск контейнера на Railway (и большинстве PaaS) **эфемерный**: при каждом
редеплое или перезапуске сервиса файл SQLite создаётся заново, и весь
сохранённый словарь пользователей пропадает. Чтобы этого избежать:

1. В Railway откройте сервис → **Settings → Volumes → New Volume**, укажите
   путь монтирования, например `/data`.
2. Добавьте переменную `DATABASE_PATH=/data/bot.db` во вкладке **Variables**.
3. Передеплойте сервис — теперь файл БД лежит на постоянном диске и
   переживает редеплои.

Без подключённого volume бот продолжит работать нормально — просто словарь
будет сбрасываться при каждом обновлении кода на Railway. Для локального
запуска и Docker без volume это не проблема — файл просто лежит на диске
компьютера/контейнера между запусками.

### Обновление бота после изменений в коде

- Вариант A: просто сделайте `git push` в тот же репозиторий — Railway
  задеплоит новую версию автоматически.
- Вариант B: повторите `railway up` из папки проекта.

### Локальная проверка Docker-образа (по желанию)

Перед деплоем можно проверить, что контейнер собирается и запускается локально:

```bash
docker build -t georgian-translator-bot .
docker run --env-file .env georgian-translator-bot
```

## Как это работает

### Режим GEO Translit ➡️ RU: двухэтапный пайплайн со смарт-маршрутизацией

Реализовано в `translate_geo_to_ru_smart()` (`main.py`), три этапа:

1. **Локальная транслитерация** (`transliteration.transliterate_to_mkhedruli`)
   — чистая Python-функция без обращения к каким-либо API: жадно сканирует
   текст, сначала пробуя известные буквосочетания (`dz→ძ`, `ts→ც`, `ch→ჩ`,
   `sh→შ`, `gh→ღ`, `kh→ხ`, `zh→ჟ`), затем одиночные буквы. Georgian
   транслитерация принципиально неоднозначна (например, «ts» может значить и
   ც, и წ; «ch» — и ჩ, и ჭ) — для каждой такой пары в коде зафиксирован один
   дефолтный вариант; для текста, где это важно, неоднозначность распутывает
   уже Gemini на следующем этапе (см. промт `SYSTEM_PROMPT_GEO_TO_RU_COMPLEX`).
2. **Оценка сложности** (`smart_routing.is_simple_text`) — эвристика (не ИИ):
   простой текст — точное совпадение с частым бытовым выражением
   (gamarjoba, madloba, rogor xar, ki, ara и т.п.) либо короткая фраза (до 5
   слов) без цифр, «официальной» пунктуации (скобки, кавычки, `:`/`;`) и без
   нескольких запятых. Всё остальное считается сложным.
3. **Выбор провайдера**:
   - простой текст → бесплатный **Google Translate** (`deep_translator`),
     мгновенно и без расхода квоты Gemini;
   - сложный текст → **Gemini** (`gemini-3.6-flash`) с промтом
     `SYSTEM_PROMPT_GEO_TO_RU_COMPLEX`, который на вход получает уже
     мхедрули-текст и должен понять контекст, при необходимости исправить
     неоднозначности транслитерации и перевести на русский.
   - **Фоллбэк в обе стороны**: если Gemini недоступен или вернул ошибку
     (в т.ч. `429`), автоматически переключаемся на Google Translate; если
     Google Translate недоступен для простой фразы — пробуем Gemini. Если
     отказали оба провайдера, пользователю показывается исходная ошибка
     (в т.ч. специальное сообщение про лимит, если проблема была именно в
     квоте Gemini).

Тональность (`/settings`) применяется только на "сложной" Gemini-ветке —
Google Translate не принимает стилистические инструкции.

### Режимы RU ➡️ GEO Translit и GEO Translit ➡️ ქართული

Работают как раньше — прямой вызов Gemini с соответствующим системным
промтом (`build_system_prompt` в `modes.py`), без смарт-маршрутизации: это
не задача "перевод бытовой фразы", здесь Gemini нужен всегда.

### Кнопки «Исходный текст (Мхедрули)», «Разбор», «Сохранить»

Все три кнопки читают данные последнего перевода под конкретным сообщением
из кэша в памяти процесса — `pending_translations` в `main.py`, ключ
`(chat_id, message_id)`, ограничен 2000 записями (старые вытесняются).
Хранит исходный текст, перевод и (для режима 1) уже посчитанный мхедрули-текст.

- **«Исходный текст (Мхедрули)»** — просто достаёт готовый мхедрули-текст из
  кэша и присылает новым сообщением. Никакого API-вызова не требуется — Этап
  1 пайплайна уже всё посчитал при первом переводе.
- **«Разбор»** — вызывает Gemini с промтом `EXPLAIN_PROMPT`.
- **«Сохранить»** — пишет пару в SQLite.

После срабатывания «Разбор» и «Сохранить» заменяются на неактивную кнопку
(`✅ ...`, callback `noop`), чтобы повторный клик не тратил лишний запрос.

**Ограничение:** если бот перезапустится, эти три кнопки под уже
отправленными сообщениями перестанут работать (ответят подсказкой
«отправьте фразу ещё раз») — кэш живёт только в памяти процесса. Для новых
переводов после перезапуска всё работает как обычно.

### Переключение режима и тональности (`/settings`)

Присылает inline-клавиатуру: 3 кнопки режима + 2 кнопки тональности, текущий
выбор помечается ✅. Хранится в двух словарях в памяти процесса (`user_modes`,
`user_tones` в `main.py`), отдельно по `user_id`. Это самый простой вариант
для одного/нескольких пользователей; **при перезапуске бота выбор сбрасывается
на значения по умолчанию для всех**. Если бот будет использоваться многими
людьми и это станет проблемой — стоит перенести оба словаря (и
`pending_translations`) в ту же SQLite-базу, что и словарь фраз — отдельная
небольшая доработка.

## Обработка ошибок

- Все ошибки при обращении к Gemini API логируются (`logging`, уровень ERROR,
  со стектрейсом) и не приводят к падению бота.
- Пользователь при сбое получает вежливое сообщение об ошибке и может
  повторить попытку.
- Превышение квоты Gemini API (`429 RESOURCE_EXHAUSTED`) распознаётся
  отдельно (`user_facing_gemini_error` в `main.py`) и показывает пользователю
  понятное сообщение про лимит, а не общую ошибку — но в режиме (1) это
  всплывает только если И Gemini, И Google Translate одновременно недоступны
  (см. смарт-роутинг выше).
- Ошибки сохранения в словарь (БД недоступна и т.п.) логируются и не мешают
  показать перевод — пользователь просто увидит уведомление о неудачном
  сохранении.
- При отсутствии `TELEGRAM_BOT_TOKEN` или `GEMINI_API_KEY` бот завершает
  работу при старте с понятным сообщением в логах.

### Лимиты бесплатного тарифа Gemini API

У бесплатного тарифа Gemini API есть суточная квота запросов (Requests Per
Day) на комбинацию проект/модель — для новых моделей вроде `gemini-3.6-flash`
она может быть очень маленькой (например, 20 запросов в сутки). Смарт-роутинг
в режиме (1) снижает расход квоты (простые фразы вообще не доходят до
Gemini), но режимы (2) и (3), а также кнопка «Разбор», по-прежнему тратят
запрос на каждое обращение. Квота обновляется в полночь по тихоокеанскому
времени (США). Текущие лимиты для конкретного ключа/модели видны на
[странице лимитов в Google AI Studio](https://aistudio.google.com/rate-limit).

Если бот нужен для реального ежедневного использования, а не для тестов —
стоит подключить платный (pay-as-you-go) тариф в Google AI Studio /
Google Cloud Console: лимиты вырастают на порядки, а стоимость для такого
лёгкого текстового бота обычно составляет доли цента за перевод.

### Ограничения Google Translate в этом пайплайне

`deep_translator` использует неофициальный публичный интерфейс
`translate.google.com` — бесплатно и без ключа, но без SLA: Google может
временно ограничить частоту запросов с одного IP при высокой нагрузке.
Кроме того, Google Translate переводит ровно тот мхедрули-текст, который ему
передали — если локальная транслитерация ошиблась на неоднозначной букве
(см. `transliteration.py`), Google Translate это не исправит (в отличие от
Gemini на "сложной" ветке, которая умеет поправить смысл по контексту). Это
осознанный компромисс архитектуры: экономия квоты Gemini на бытовых фразах в
обмен на чуть меньшую точность на редких неоднозначных случаях.

## Заметки

- Модель Gemini вызывается асинхронно (`client.aio.models.generate_content`),
  чтобы не блокировать event loop aiogram; `deep_translator` — синхронная
  библиотека, поэтому её вызов оборачивается в `asyncio.to_thread`.
- Все системные промты (3 режима перевода, `EXPLAIN_PROMPT`, `OFFICIAL_STYLE`,
  `CASUAL_STYLE`) собраны в `prompts.py` — их можно скорректировать под свои
  нужды в одном месте.
- Список "простых" бытовых фраз для смарт-роутинга — в `smart_routing.py`
  (`_SIMPLE_PHRASES_LATIN`), можно дополнить своими примерами.
- Записи словаря удаляются с проверкой владельца (`user_id`) — один
  пользователь не может удалить запись другого, даже если бы подобрал её id.
- Сообщения, начинающиеся с `/` и не совпадающие с известными командами, бот
  не отправляет в Gemini, а отвечает подсказкой по доступным командам.